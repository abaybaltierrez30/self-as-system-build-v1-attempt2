I want to build an interactive visual system based on the work I did in my Visual Rule Test project combining test 1 and 2 with the atmospheric circles in 4.
This isn't looking the way I want it to. I was opting for more of a menu as in Windows/macOS application window interfaces, with buttons (like in Test 1). I'd like to see it become a full on window-built menu, with a primary purple-salmon tint like in Test 1, buttons like in Test 1, animation when things are highlighted like in tests 2-4, and also the transparent background circles surrounding the windows like in test 4.
The menu isn't looking full-on yet. I want to revamp the whole interface, so that the whole webpage becomes the menu instead of it being visual rule tests with a menu that controls the tests. There will be a 2-second animation when launched that says "WELCOME TO THE DEMO MENU!" and a plain starter window with 8 (currently blank buttons with no function). Make sure to keep the same tint, button, animation, and atmospheric circle inspo as the previous update, and make sure to also add the circles surrounding the window that were in the Atmospheric Circles test into the exterior of the windows.